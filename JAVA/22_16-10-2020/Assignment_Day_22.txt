Day 22

1. Create a distributed application (using TCP/IP) for the following.
StudentManagementService
Client sends subject to server (in string form)
Server (using collections: student details added in a map) sends the list of students(enrolled in a subject) with distinction (gpa > 7) to the client.
If Subject name is valid , return list of students for the specified subject. 
Otherwise return suitable error message.

In client side application : display error message or the list.

2. Check JDBC connectivity & write simple JDBC application to display emp details.	










